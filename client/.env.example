# Optional. Leave unset during local dev: Vite proxies /api → http://127.0.0.1:3001
# (same host = works on phone at http://YOUR-PC-IP:5173 without CORS issues).
# Required for production static build: your public API origin, no trailing slash.
# VITE_API_URL=https://your-api.onrender.com
